#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        string curr_place;
        cin>>curr_place;
        char alph = curr_place[0];
        int place_val = curr_place[1]-'0';
        //cout<<place_val;
        for(int i=1;i<=8;i++)
        {
            if(i!=place_val)
            {
                cout<<alph<<i<<endl;
            }
        }
        for(char i='a';i<='h';i++)
        {
            if(i!=alph)
            {
                cout<<i<<place_val<<endl;
            }
        }
    }
}
